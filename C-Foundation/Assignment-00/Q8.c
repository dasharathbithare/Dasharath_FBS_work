//Q8: Write a C program to input the lenght and width of a reactangle and finds its perimeter.

#include <stdio.h>
int main ()
{
	int l = 8, w = 5;
	int p = 2*(l+w);
	
	printf("The perimeter of reactangle is %d", p);
	return 0;
	 
}